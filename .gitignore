# Never commit these (CLAUDE.md, "Environment").
models/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.npz
data/*.joblib
data/hidden/
# Regenerated by scripts/make_test_audio.py in a few seconds; ~1 MB of wavs
# that would otherwise churn in every diff.
data/test_audio/
# Tracked on purpose, both of them:
#   data/probe/*.jsonl      the probe's seed dataset
#   data/layer_sweep.json   which layer was chosen and why. Small, and the
#                           only machine-readable record of the measurement --
#                           without it a fresh clone loses the chosen L.
.venv/
.venv-*/
.env
logs/

__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
